<h1>Needs</h1>

<table class="table">
  <tr>
    <th>Description</th>
    <th>Person</th>
  </tr>

<% @needs.each do |need| %>
  <tr>
    <td><%= need.description %></td>
    <td><%= need.user.email %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Need', new_need_path %>
